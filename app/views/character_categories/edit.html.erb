<h1>Editing Character Category</h1>

<%= render 'form', character_category: @character_category %>

<% add_to_footer('Show', @character_category) %>
<% add_to_footer('Back', character_categories_path) %>
