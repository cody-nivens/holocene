<p>
  <strong>Actor:</strong>
  <%= @actor_character.actor.name %>
</p>

<p>
  <strong>Character:</strong>
  <%= @actor_character.character.name %>
</p>

<div id="side_controls" class="hide-print">
<%= link_to (fa_icon 'edit', class: 'fa-fw'), edit_actor_character_path(@actor_character), class: "btn", title: 'Edit' %>
<%= link_to (fa_icon 'backward', class: 'fa-fw'), actor_actor_characters_path(@actor), class: "btn", title: 'Back' %>
</div>
