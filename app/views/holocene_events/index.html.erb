<h1>Holocene Events</h1>

<%= datagrid_form_for @grid, :method => :get, :url => holocene_events_path %>

<%= paginate(@grid.assets) %>
<%= datagrid_table @grid %>
<%= paginate(@grid.assets) %>

<% @footer_content << "#{link_to 'New Holocene event', new_holocene_event_path, :class => "btn btn-primary"}" %>
