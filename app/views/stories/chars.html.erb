<% @title = "Chars#{params[:outline].nil? ? '' : ' Outline'}, #{@story.name}" %>
<%= render partial: 'stories/header', locals: { book: @book, story: @story } %>
<%= render partial: 'shared/chars', locals: { object: @story, op: op } %>
<%= render partial: 'stories/footer', locals: { book: @book, story: @story } %>

