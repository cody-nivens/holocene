<h2>Editing Segment</h2>

<turbo-frame id="<%= dom_id @segment  %>">
<%= render 'form', segment: @segment %>
<%= link_to (fa_icon 'backward', class: 'fa-fw'),  return_or_default_path(stage_segments_path(@stage)), class: "btn", title: 'Back' %>
</turbo-frame>
