<p>
  <strong>Name:</strong>
  <%= @timeline.name %>
</p>

  <%== @timeline.description %>

<%= datagrid_form_for @grid, method: :get, url: holocene_events_path %>

<%= paginate(@grid.assets) %>
<%= datagrid_table @grid %>
<%= paginate(@grid.assets) %>

<div id="side_controls" class="hide-print">
<%= link_to (fa_icon 'edit', class: 'fa-fw'), edit_timeline_path(@timeline), class: 'btn', title: 'Edit' %>
<%= link_to (fa_icon 'stream', class: 'fa-fw'), timeline_timeline_path(timeline_id: @timeline.id), class: 'btn', title: 'Timeline' %>
<%= link_to (fa_icon 'list-alt', class: 'fa-fw'), timeline_display_path(timeline_id: @timeline.id), class: 'btn', title: 'Display' %>
<%= link_to (fa_icon 'map', class: 'fa-fw'), geo_map_timeline_path(@timeline), class: 'btn', title: 'Map' %>
<%= link_to (fa_icon 'backward', class: 'fa-fw'), timelines_path, class: 'btn', title: 'Back' %>
</div>
