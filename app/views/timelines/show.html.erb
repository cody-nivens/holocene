<p>
  <strong>Name:</strong>
  <%= @timeline.name %>
</p>

  <%== @timeline.description %>

<%= datagrid_form_for @grid, method: :get, url: holocene_events_path %>

<%= paginate(@grid.assets) %>
<%= datagrid_table @grid %>
<%= paginate(@grid.assets) %>

<% add_to_footer('Edit', edit_timeline_path(@timeline)) %>
<% add_to_footer('Timeline', timeline_timeline_path(timeline_id: @timeline.id)) %>
<% add_to_footer('Display', timeline_display_path(timeline_id: @timeline.id)) %>
<% add_to_footer('Map', geo_map_timeline_path(@timeline)) %>
<% add_to_footer('Back', timelines_path) %>
