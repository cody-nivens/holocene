<p>
  <strong>Name:</strong>
  <%= @timeline.name %>
</p>

  <%== @timeline.description %>

<%= datagrid_form_for @grid, :method => :get, :url => holocene_events_path %>

<%= paginate(@grid.assets) %>
<%= datagrid_table @grid %>
<%= paginate(@grid.assets) %>

<% @footer_content << "#{link_to 'Edit', edit_timeline_path(@timeline), :class => "btn btn-default"}" %>
<% @footer_content << "#{link_to 'Timeline', timeline_timeline_path(:timeline_id => @timeline.id), :class => "btn btn-default"}" %>
<% @footer_content << "#{link_to 'Display', timeline_display_path(:timeline_id => @timeline.id), :class => "btn btn-default"}" %>
<% @footer_content << "#{link_to 'Map', geo_map_timeline_path(@timeline), :class => "btn btn-default"}" %>
<% @footer_content << "#{link_to 'Back', :back, :class => "btn btn-default"}" %>
