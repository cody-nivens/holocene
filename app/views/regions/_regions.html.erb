<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Events</th>
      <th>Body</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @regions.each do |region| %>
      <turbo-frame id="<%= dom_id region %>">
        <%= render partial: "regions/region_item", locals: { region: region } %>
      </turbo-frame>
    <% end %>
  </tbody>
</table>
