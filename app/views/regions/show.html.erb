<p>
  <strong>Name:</strong>
  <%= @region.name %>
</p>

<p>
  <strong>Body:</strong>
  <%= @region.body %>
</p>

<%= datagrid_form_for @grid, method: :get, url: holocene_events_path %>

<%= paginate(@grid.assets) %>
<%= datagrid_table @grid %>
<%= paginate(@grid.assets) %>

<div id="side_controls" class="hide-print">
<%= link_to (fa_icon 'edit', class: 'fa-fw'), edit_region_path(@region), class: 'btn', title: 'Edit' %>
<%= link_to (fa_icon 'backward', class: 'fa-fw'), regions_path, class: 'btn', title: 'Back' %>
</div>
