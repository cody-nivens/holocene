<h2>Editing Region</h2>

<%= render 'form', region: @region, command_label: 'Update Region' %>

<% add_to_footer('Show', @region) %>
<% add_to_footer('Back',  return_or_default_path(regions_path)) %>
