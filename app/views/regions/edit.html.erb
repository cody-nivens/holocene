<h1>Editing Region</h1>

<%= render 'form', region: @region, command_label: "Update Region"  %>

<% @footer_content << "#{link_to 'Show', @region}" %>
<% @footer_content << "#{link_to 'Back', regions_path}" %>
