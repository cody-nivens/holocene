<%= bootstrap_form_for(region, local: true) do |form| %>
  <% if region.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(region.errors.count, "error") %> prohibited this region from being saved:</h2>

      <ul>
      <% region.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

      <%= form.hidden_field :user_id %>
    <%= form.text_field :name %>

    <%= form.rich_text_area :body, cols: "80", rows: "15", hide_label: true %>

  <%= form.primary "#{command_label}" %>
<% end %>
