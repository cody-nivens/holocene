<p id="notice"><%= notice %></p>

<p>
<strong><%= @aside.name %></strong>
</p>

<%== @aside.body %>

<% @footer_content << (link_to 'Edit', edit_aside_path(@aside), class: 'btn btn-default').to_s %>
<% @footer_content << (link_to 'Back', chapter_asides_path(@chapter), class: 'btn btn-default').to_s %>
