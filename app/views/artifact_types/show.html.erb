<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @artifact_type.name %>
</p>

<p>
  <strong>Location:</strong>
  <%= @artifact_type.location %>
</p>

<p>
  <strong>Book:</strong>
  <%= @artifact_type.book_id %>
</p>

<% @footer_content << (link_to 'Edit', edit_artifact_type_path(@artifact_type), class: 'btn btn-default').to_s %>
<% @footer_content << (link_to 'Back', book_artifact_types_path(@book), class: 'btn btn-default').to_s %>
