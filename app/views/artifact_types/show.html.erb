<p>
  <strong>Name:</strong>
  <%= @artifact_type.name %>
</p>

<p>
  <strong>Location:</strong>
  <%= @artifact_type.location %>
</p>

<p>
  <strong>Book:</strong>
  <%= @artifact_type.book_id %>
</p>

<div id="side_controls" class="hide-print">

<%= link_to (fa_icon 'edit', class: 'fa-fw'), edit_artifact_type_path(@artifact_type), class: 'btn', title: 'Edit' %>
<%= link_to (fa_icon 'backward', class: 'fa-fw'), book_artifact_types_path(@book), class: 'btn', title: 'Back' %>
