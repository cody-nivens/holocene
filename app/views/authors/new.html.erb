<h1>New Author</h1>

<%= render 'form', user: current_user, author: @author, command_label: "Create Author"  %>

<% @footer_content << "#{link_to 'Back', book_authors_path(@book), :class => "btn btn-default"}" %>
