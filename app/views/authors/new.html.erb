<h2>New Author</h2>

<%= render 'form', user: current_user, author: @author, command_label: 'Create Author', object: @object %>

<% add_to_footer('Back',  return_or_default_path(polymorphic_path([@object, :authors]))) %>
