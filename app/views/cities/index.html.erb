<h1>Cities</h1>
<%= datagrid_form_for @grid, :method => :get, :url => cities_path, :partials => 'city_grid' %>

<%= paginate(@grid.assets) %>
<%= datagrid_table @grid, :partials => 'city_grid' %>
<%= paginate(@grid.assets) %>

<% @footer_content << "#{link_to 'New City', new_city_path, :class => "btn btn-default"}" %>
