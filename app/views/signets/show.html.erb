<p id="notice"><%= notice %></p>

<p>
  <strong>Color:</strong>
  <%= @signet.color %>
</p>

<p>
  <strong>Message:</strong>
  <%= @signet.message %>
</p>

<% @footer_content << (link_to 'Edit', edit_signet_path(@signet), class: 'btn btn-default').to_s %>
<% @footer_content << (link_to 'Back', polymorphic_path(@sigged), class: 'btn btn-default').to_s %>
