<% @title = "Signets" %>
<h2>Signets - <%= @sigged.class.name %>: <%= @sigged.name %></h2>

<div>
<%= link_to (fa_icon 'plus'), new_polymorphic_path([@sigged, :signet]), title: 'New Signet' %>
</div>
<% if @sigged.signets.size > 0 %>
<table class='table'>
  <thead>
    <tr>
      <th>Signet</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @sigged.signets.reload.each do |signet| %>
      <tr>
        <%= render partial: 'signets/signet_line', locals: { signet: signet, sigged: @sigged } %>
      </tr>
    <% end %>
  </tbody>
</table>

<% end %>

