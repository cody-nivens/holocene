<h2>Editing Signet - <%= @sigged.class.name %>: <%= @sigged.name %></h2>

<%= render 'form', signet: @signet, sigged: @sigged %>

<% add_to_footer('Show', polymorphic_path([@sigged, @signet])) %>
<% add_to_footer('Back',  return_or_default_path(polymorphic_path(@sigged))) %>
