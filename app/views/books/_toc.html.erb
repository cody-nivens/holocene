<% @chapters.includes([:rich_text_body, :sections, :holocene_events, :chapters_holocene_events, { sections: [:holocene_events, :holocene_events_sections, :rich_text_body] }, { partition: :rich_text_body } ]).order(:position).each do |chapter| %>
  <%= render partial: 'chapters/chapter_item', locals: { chapter: chapter } %>
<% end %>
