<h1>
  <%= @book.name %> | <%= link_to 'Edit', edit_book_path(@book), :class => "btn btn-primary" %>
</h1>

<p>
  <%= raw @book.body %>
</p>

<%= render 'chapters/index', book_id: @book.id %>

<% @footer_content << "#{link_to 'Edit', edit_book_path(@book), :class => "btn btn-primary" }" %>
<% @footer_content << "#{link_to 'Chapters', book_chapters_path(@book), :class => "btn btn-primary" }" %>
<% @footer_content << "#{link_to 'Back', books_path, :class => "btn btn-primary" }" %>
