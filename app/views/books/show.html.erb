<h1>
  <%= @book.name %>  <%= link_to 'Edit', edit_book_path(@book), :class => "btn btn-default" %>
</h1>

<p>
  <%= raw @book.body %>
</p>

<%= render 'chapters/index', book_id: @book.id %>

<% @footer_content << "#{link_to 'Edit', edit_book_path(@book), :class => "btn btn-default" }" %>
<% @footer_content << "#{link_to 'New Chapter', new_book_chapter_path(@book), :class => "btn btn-default" }" %>
<% @footer_content << "#{link_to 'Chapters', book_chapters_path(@book), :class => "btn btn-default" }" %>
