<h1>New Character Scene</h1>

<%= render 'form', character_scene: @character_scene, scene: @scene %>

<% @footer_content << "#{link_to 'Back', polymorphic_path([@character_scene.scene, :character_scenes]), :class => "btn btn-default"}" %>
