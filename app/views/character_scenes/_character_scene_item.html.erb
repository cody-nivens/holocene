<turbo-frame id="<%= dom_id character_scene %>" class="block">
  <%= render partial: "characters/character_item", locals: { character: character_scene.character, object: character_scene.scene } %>
</turbo-frame>

