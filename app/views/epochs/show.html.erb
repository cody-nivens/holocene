<p>
<strong>Name:</strong>
  <%= @epoch.name %><br/>
  <strong>Start date:</strong>
  <%= @epoch.start_date %><br/>
  <strong>End date:</strong>
  <%= @epoch.end_date %>
</p>

<%= datagrid_form_for @grid, :method => :get, :url => holocene_events_path %>

<%= paginate(@grid.assets) %>
<%= datagrid_table @grid %>
<%= paginate(@grid.assets) %>

<% @footer_content << "#{link_to 'Edit', edit_epoch_path(@epoch), :class => "btn btn-default"}" %>
<% @footer_content << "#{link_to 'Timeline', epoch_timeline_path(@epoch), :class => "btn btn-default"}" %>
<% @footer_content << "#{link_to 'Map', geo_map_epoch_path(@epoch), :class => "btn btn-default"}" %>
<% @footer_content << "#{link_to 'Back', epochs_url, :class => "btn btn-default"}" %>
