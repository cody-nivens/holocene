<p>
<strong>Name:</strong>
  <%= @epoch.name %><br>
  <strong>Start date:</strong>
  <%= @epoch.start_date %><br>
  <strong>End date:</strong>
  <%= @epoch.end_date %>
</p>

<%= datagrid_form_for @grid, method: :get, url: holocene_events_path %>

<%= paginate(@grid.assets) %>
<%= datagrid_table @grid %>
<%= paginate(@grid.assets) %>

<div id="side_controls" class="hide-print">
<%= link_to (fa_icon 'edit', class: 'fa-fw'), edit_epoch_path(@epoch), class: 'btn', title: 'Edit' %>
<%= link_to (fa_icon 'stream', class: 'fa-fw'), epoch_timeline_path(@epoch), class: 'btn', title: 'Timeline' %>
<%= link_to (fa_icon 'map', class: 'fa-fw'), geo_map_epoch_path(@epoch), class: 'btn', title: 'Map' %>
<%= link_to (fa_icon 'backward', class: 'fa-fw'), epochs_url, class: 'btn', title: 'Back' %>
</div>
