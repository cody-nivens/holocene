<p>
  <strong>Name:</strong>
  <%= @epoch.name %>
</p>

<p>
  <strong>Start date:</strong>
  <%= @epoch.start_date %>
</p>

<p>
  <strong>End date:</strong>
  <%= @epoch.end_date %>
</p>

<%= datagrid_form_for @grid, :method => :get, :url => holocene_events_path %>

<%= paginate(@grid.assets) %>
<%= datagrid_table @grid %>
<%= paginate(@grid.assets) %>

<% @footer_content << "#{link_to 'Edit', edit_epoch_path(@epoch)}" %>
<% @footer_content << "#{link_to 'Timeline', epoch_timeline_path(@epoch)}" %>
<% @footer_content << "#{link_to 'Back', epochs_path}" %>
