json.extract! scene, :id, :abc, :check, :summary, :place, :time, :scene_sequel, :goal_reaction, :conflict_dilemma, :disaster_decision, :short_term_goal, :long_term_goal, :over_arching_goal, :created_at, :updated_at
json.url scene_url(scene, format: :json)
