$("#scene-checkbox-<%= @scene.id %>").html(
   <%= check_box_tag 'check', !@scene.check, @scene.check,
              :data => {:remote => true,
                        :url => url_for( :controller => 'scenes', :action => 'check', :id => @scene.id),
                        :method => :put},
                        :class => 'input-large' %>

);


