<%= (scene.section.nil? ? '*' : ' ') %> <%= (pdf.nil? ? link_to(scene.abc, polymorphic_path([scripted,key_point, scene])) : scene.abc) %> <%= scene.time_to_text %> : <%= scene.characters.count %> : <%= scene.word_count %> : <%= (scene.artifact.nil? ? "Unknown" : scene.artifact.name) %><%= (scene.place.empty? ? '' : " - #{scene.place.to_plain_text}") %> <br/> <%= scene.name %>
