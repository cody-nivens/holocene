<%= (pdf.nil? ? link_to(scene.abc, polymorphic_path([scripted,key_point, scene])) : scene.abc) %><%= (scene.section.nil? ? '*' : ' ') %> <%= scene.time_to_text %> : <%= scene.characters.count %> : <%= scene.word_count %> : <%= scene.place.to_plain_text %>
