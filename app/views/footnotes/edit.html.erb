<h1>Editing Footnote</h1>

<%= render 'form', footnote: @footnote, command_label: 'Update Footnote', noted: @noted %>

<% add_to_footer('Show', polymorphic_path([@noted, @footnote])) %>
<% add_to_footer('Back', polymorphic_path([@noted, :footnotes])) %>
