<h2>New Footnote</h2>

<%= render 'form', footnote: @footnote, command_label: 'Create Footnote', noted: @noted %>

<% add_to_footer('Back',  return_or_default_path(polymorphic_path([@noted, :footnotes]))) %>
