<h2>Editing Itinerary</h2>

<%= render 'form', itinerary: @itinerary, cities: @cities %>

<% add_to_footer('Show', itinerary_path(@itinerary)) %>
<% add_to_footer('Back',  return_or_default_path(tour_path(@tour))) %>
