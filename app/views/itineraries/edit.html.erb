<h1>Editing Itinerary</h1>

<%= render 'form', itinerary: @itinerary, cities: @cities %>

<% @footer_content << (link_to 'Show', itinerary_path(@itinerary), class: 'btn btn-default hide-print').to_s %>
<% @footer_content << (link_to 'Back', tour_path(@tour), class: 'btn btn-default hide-print').to_s %>
