<h1>Editing Character Value</h1>

<%= render 'form', character_value: @character_value %>

<%= link_to 'Show', @character_value %> |
<%= link_to 'Back', character_values_path(:character_id => @character.id) %>
