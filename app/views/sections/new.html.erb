<h1>New Section</h1>

<%= render 'form', scripted: @scripted, chapter: @chapter, section: @section, command_label: "Create Section" %>

<% @footer_content << "#{link_to 'Back', chapter_sections_path(@chapter), :class => "btn btn-default"}" %>
