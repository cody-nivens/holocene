<%= turbo_stream.replace "#{dom_id @section}" do %>
  <% sectioned = @section.sectioned %>
  <%= render partial: "#{sectioned.class.name.underscore}s/#{sectioned.class.name.underscore}_item", locals: { "#{sectioned.class.name.underscore}": @section.sectioned } %>
<% end %>
<%= render partial: "shared/send_flash" %>
