<h2>Editing Section</h2>

<turbo-frame id="<%= dom_id @section %>">
  <%= render partial: 'form', locals: { sectioned: @sectioned, section: @section, command_label: 'Update Section' } %>
</turbo-frame>
<%= link_to (fa_icon 'backward', class: 'fa-fw'),  polymorphic_path(@sectioned), class: 'btn', title: 'Back' %>
