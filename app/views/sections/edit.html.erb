<h1>Editing Section</h1>

<%= render 'form', scripted: @scripted, sectioned: @sectioned, section: @section, command_label: "Update Section"  %>

<% @footer_content << "#{link_to 'Show', polymorphic_path([@sectioned, @section]), :class => "btn btn-default"}" %>
<% @footer_content << "#{link_to 'Back', polymorphic_path(@sectioned), :class => "btn btn-default"}" %>
