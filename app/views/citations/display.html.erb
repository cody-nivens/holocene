<p>
  <strong>Name:</strong>
  <%= @citation.name %>
</p>

<%= link_to 'Add Event', citation_holocene_events_path(@citation) %> |
<%= datagrid_form_for @grid, :method => :get, :url => holocene_events_path %>

<%= paginate(@grid.assets) %>
<%= datagrid_table @grid %>
<%= paginate(@grid.assets) %>

<div class="navbar-fixed-bottom footer">
<%= link_to 'Edit', edit_biblioentry_citation_path(@citation.biblioentry,@citation) %> |
<%= link_to 'Timeline', citation_timeline_path(@citation) %> |
<%= link_to 'Back', biblioentry_citations_path(@citation.biblioentry) %>
</div>
