json.extract! character, :id, :name, :reason_for_name, :nickname, :reason_for_nickname, :ethnicity, :occupation_class, :social_class, :created_at, :updated_at
json.url character_url(character, format: :json)
