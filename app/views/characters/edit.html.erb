<h1>Editing Character</h1>

<%= render 'form', character: @character, year: @year, object: @object %>

<% @footer_content << "#{link_to 'Show', character_path(@character), :class => "btn btn-default" }" %>
<% @footer_content << "#{link_to 'Back', polymorphic_path([@object, :characters]), :class => "btn btn-default" }" %>
