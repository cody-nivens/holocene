<h2><%= @partition.name %></h2>

<p>
  <%== @partition.body %>
</p>

<div id="side_controls" class="hide-print">

<%= link_to (fa_icon 'edit', class: 'fa-fw'), edit_partition_path(@partition), class: 'btn', title: 'Edit' %>
<%= link_to (fa_icon 'backward', class: 'fa-fw'), chapter_path(@partition.chapter), class: 'btn', title: 'Back' %>
</div>
