<p id="notice"><%= notice %></p>

<h1>
  <%= @partition.name %>
</h1>

<p>
  <%== @partition.body %>
</p>

<% @footer_content << (link_to 'Edit', edit_partition_path(@partition), class: 'btn btn-default').to_s %>
<% @footer_content << (link_to 'Back', chapter_path(@partition.chapter), class: 'btn btn-default').to_s %>
