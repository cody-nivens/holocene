<%= render 'form', scripted: @scripted, chapter: @chapter, command_label: 'Update Chapter' %>

<% @footer_content << (link_to 'Show', chapter_path(@chapter), class: 'btn btn-default').to_s %>
<% @footer_content << (link_to 'Back', polymorphic_path([@scripted, :chapters]), class: 'btn btn-default').to_s %>
