<h1>Editing Chapter</h1>

<%= render 'form', book: @book, chapter: @chapter, command_label: "Update Chapter"  %>

<% @footer_content << "#{link_to 'Show', book_chapter_path(@book, @chapter)}" %>
<% @footer_content << "#{link_to 'Back', book_chapters_path(@book)}" %>
