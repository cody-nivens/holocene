<h1><%= @event_type.name %></h1>

<p>
  <%= @event_type.body %>
</p>

<%= datagrid_form_for @grid, :method => :get, :url => holocene_events_path %>

<%= paginate(@grid.assets) %>
<%= datagrid_table @grid %>
<%= paginate(@grid.assets) %>


<% @footer_content << "#{link_to 'Edit', edit_event_type_path(@event_type)}" %>
<% @footer_content << "#{link_to 'Back', event_types_path}" %>
