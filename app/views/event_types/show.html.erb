<h1><%= @event_type.name %></h1>

<p>
  <%= @event_type.body %>
</p>

<%= datagrid_form_for @grid, method: :get, url: holocene_events_path %>

<%= paginate(@grid.assets) %>
<%= datagrid_table @grid %>
<%= paginate(@grid.assets) %>

<% @footer_content << (link_to 'Edit', edit_event_type_path(@event_type), class: 'btn btn-default').to_s %>
<% @footer_content << (link_to 'Timeline', event_type_timeline_path(@event_type), class: 'btn btn-default').to_s %>
<% @footer_content << (link_to 'Map', geo_map_event_type_path(@event_type), class: 'btn btn-default').to_s %>
<% @footer_content << (link_to 'Back', :back, class: 'btn btn-default').to_s %>
