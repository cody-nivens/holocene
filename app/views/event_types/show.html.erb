<h2><%= @event_type.name %></h2>

<p>
  <%= @event_type.body %>
</p>

<%= datagrid_form_for @grid, method: :get, url: holocene_events_path %>

<%= paginate(@grid.assets) %>
<%= datagrid_table @grid %>
<%= paginate(@grid.assets) %>

<div id="side_controls" class="hide-print">
<%= link_to (fa_icon 'edit', class: 'fa-fw'), edit_event_type_path(@event_type), class: 'btn', title: 'Edit' %>
<%= link_to (fa_icon 'stream', class: 'fa-fw'), event_type_timeline_path(@event_type), class: 'btn', title: 'Timeline' %>
<%= link_to (fa_icon 'map', class: 'fa-fw'), geo_map_event_type_path(@event_type), class: 'btn', title: 'Map' %>
<%= link_to (fa_icon 'backward', class: 'fa-fw'), event_types_path, class: 'btn', title: 'Back' %>
</div>
