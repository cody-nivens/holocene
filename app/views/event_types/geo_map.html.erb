<%= render 'shared/map', object: @object %>
<% @footer_content << (link_to 'Display', event_type_path(@event_type), class: 'btn btn-default').to_s %>
<% @footer_content << (link_to 'Back', event_type_path(@event_type), class: 'btn btn-default').to_s %>
