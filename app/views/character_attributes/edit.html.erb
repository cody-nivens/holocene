<h1>Editing Character Attribute</h1>

<%= render 'form', character_attribute: @character_attribute, character_category: @character_category %>

<% @footer_content << "#{link_to 'Show', @character_attribute, :class => "btn btn-default"}" %>
<% @footer_content << "#{link_to 'Back', character_category_character_attributes_path(@character_category), :class => "btn btn-default"}" %>
