<h2>Editing Actor</h2>

<turbo-frame id="<%= dom_id @actor %>">
<%= render 'form', actor: @actor %>
<%= link_to (fa_icon 'backward', class: 'fa-fw'), :back, class: "btn", title: 'Back' %>
</turbo-frame>
