<tr>
  <% grid.html_columns(*options[:columns]).each do |column| %>
    <th class="<%= datagrid_column_classes(grid, column) %>">
      <%= column.header %>
      <% unless column.options[:description].blank?   %>
        <%=  link_to fa_icon('question'), "#", title: column.options[:description], data: { "original-title" => column.options[:description], toggle: "tooltip", placement: "top", container: "body"} %>
      <% end %>
      <%= datagrid_order_for(grid, column, options) if column.supports_order? && options[:order]%>
    </th>
  <% end %>
</tr>
