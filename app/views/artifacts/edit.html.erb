<h2>Editing Artifact</h2>

<turbo-frame id="new_object">
  <%= render 'form', artifact: @artifact, book: @book %>
  <%= link_to (fa_icon 'backward', class: 'fa-fw'),  book_artifacts_path(@book), method: :get, class: 'btn', title: 'Back' %>
</turbo-frame>
