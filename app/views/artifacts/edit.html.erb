<h1>Editing Artifact</h1>

<%= render 'form', artifact: @artifact, book: @book %>

<% @footer_content << "#{link_to 'Show', artifact_path(@artifact), :class => "btn btn-default"}" %>
<% @footer_content << "#{link_to 'Back', book_artifacts_path(@book), :class => "btn btn-default"}" %>
